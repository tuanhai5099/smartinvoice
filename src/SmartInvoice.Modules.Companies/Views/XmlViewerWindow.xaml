<Window x:Class="SmartInvoice.Modules.Companies.Views.XmlViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SmartInvoice.Modules.Companies.Views"
        Title="Xem XML" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Language="vi-VN">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Padding="8,6" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" BorderThickness="0,0,0,1">
            <TextBlock x:Name="PathText" Text="" FontSize="12" TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, ElementName=PathText}" />
        </Border>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="8,4">
            <Button x:Name="ExpandAllButton" Content="Mở rộng tất cả" Padding="10,4" Margin="0,0,8,0" Click="ExpandAllButton_Click" ToolTip="Mở rộng toàn bộ thẻ (tag)" />
            <Button x:Name="CollapseAllButton" Content="Thu gọn tất cả" Padding="10,4" Margin="0,0,8,0" Click="CollapseAllButton_Click" ToolTip="Thu gọn toàn bộ thẻ (chỉ còn tag gốc)" />
        </StackPanel>
        <TabControl x:Name="XmlTabs" Grid.Row="2" Margin="4">
            <TabItem Header="Cây thư mục (đóng/mở tag)">
                <Grid>
                    <TreeView x:Name="XmlTree" Background="White" BorderThickness="0" Padding="4">
                        <TreeView.Resources>
                            <HierarchicalDataTemplate DataType="{x:Type local:XmlNodeItem}" ItemsSource="{Binding Children}">
                                <StackPanel Orientation="Horizontal" ToolTip="{Binding ToolTipText}">
                                    <TextBlock Text="{Binding DisplayName}" FontFamily="Consolas" FontSize="12" />
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.Resources>
                    </TreeView>
                </Grid>
            </TabItem>
            <TabItem Header="Nguồn (XML text)">
                <TextBox x:Name="XmlText" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="12" Padding="8" Margin="4" />
            </TabItem>
        </TabControl>
    </Grid>
</Window>
