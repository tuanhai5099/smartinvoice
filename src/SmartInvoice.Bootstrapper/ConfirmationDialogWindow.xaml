<Window x:Class="SmartInvoice.Bootstrapper.ConfirmationDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title, RelativeSource={RelativeSource Self}}"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        MinWidth="360">
    <Border Padding="16"
            Background="{DynamicResource SiPageBg}">
        <StackPanel>
            <TextBlock Text="{Binding Message}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,16"
                       FontSize="13" />
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Hủy"
                        MinWidth="80"
                        Margin="0,0,8,0"
                        IsCancel="True"
                        Click="OnCancelClick" />
                <Button Content="Đồng ý"
                        MinWidth="80"
                        IsDefault="True"
                        Click="OnOkClick" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>

